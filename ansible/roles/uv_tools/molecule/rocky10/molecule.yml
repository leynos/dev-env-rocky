---
dependency:
  name: galaxy

driver:
  name: podman

platforms:
  - name: uv-tools-rocky10
    image: quay.io/rockylinux/rockylinux:10
    pre_build_image: true
    command: /usr/bin/sleep infinity

scenario:
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy

provisioner:
  name: ansible
  config_options:
    defaults:
      interpreter_python: /usr/bin/python3
      roles_path: ../..
    connection:
      pipelining: false
  env:
    ANSIBLE_COLLECTIONS_PATH: ${MOLECULE_PROJECT_DIRECTORY}/../../ansible_collections:~/.ansible/collections:/usr/share/ansible/collections
    ANSIBLE_LIBRARY: ${MOLECULE_PROJECT_DIRECTORY}/../../ansible_collections/packaging/tools/plugins/modules:${MOLECULE_PROJECT_DIRECTORY}/../../ansible_collections/agentic/agent_configs/plugins/modules
    ANSIBLE_MODULE_UTILS: ${MOLECULE_PROJECT_DIRECTORY}/../../ansible_collections/agentic/agent_configs/plugins/module_utils
  playbooks:
    prepare: prepare.yml
    converge: converge.yml
    verify: verify.yml

verifier:
  name: ansible

lint: |
  set -e
  ANSIBLE_COLLECTIONS_PATH=../../ansible_collections \
  ANSIBLE_LIBRARY=../../ansible_collections/packaging/tools/plugins/modules:../../ansible_collections/agentic/agent_configs/plugins/modules \
  ANSIBLE_MODULE_UTILS=../../ansible_collections/agentic/agent_configs/plugins/module_utils \
  ANSIBLE_ROLES_PATH=../.. \
    ansible-playbook --syntax-check molecule/rocky10/converge.yml
  ANSIBLE_COLLECTIONS_PATH=../../ansible_collections \
  ANSIBLE_LIBRARY=../../ansible_collections/packaging/tools/plugins/modules:../../ansible_collections/agentic/agent_configs/plugins/modules \
  ANSIBLE_MODULE_UTILS=../../ansible_collections/agentic/agent_configs/plugins/module_utils \
  ANSIBLE_ROLES_PATH=../.. \
    ansible-playbook --syntax-check molecule/rocky10/verify.yml
